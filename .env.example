# RSMate Environment Variables

# Fernet Key for encrypted session storage
# Generate a new key with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# If not set, a key will be auto-generated and stored in an env variable
RSMATE_FERNET_KEY=
